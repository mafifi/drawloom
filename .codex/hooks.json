{
  "description": "Advisory shared UI feedback after edits. Review and trust this repository hook through /hooks before use; CI remains the enforcement boundary.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "^apply_patch$",
        "hooks": [
          {
            "type": "command",
            "command": "bun \"$(git rev-parse --show-toplevel)/scripts/ui-policy-hook.ts\"",
            "timeout": 10,
            "additionalContextLimit": 1000
          }
        ]
      }
    ]
  }
}
